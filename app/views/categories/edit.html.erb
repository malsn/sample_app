<% provide(:title, 'Edit Category') %>
<h1>Edit category <%= @category.title %></h1>

<div class="row">
  <aside class="span4">
	<section>
	<% if @category.parent.blank? %>
	root
	<% else %>
	<%= link_to @category.parent.title, @category.parent %>
	<% end %>
	</section>
	<section>
	<% if @subcategories.empty? %>
	Has no child categories
	<% else %>
	<ul class="subcategories">
	  <%= render @subcategories %>
	</ul>
	<% end %>
	</section>
  </aside>
  <div class="span8">
    <%= form_for(@category) do |f| %>
	  <%= render 'fields', f: f %>
      <%= f.submit "Edit category", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>
